import { Box, Heading } from '@chakra-ui/react';
import Panel from '../../components/ui/Panel';

export const Top = () => (
  <Box p={4}>
    <Heading size="md">ホーム</Heading>
    <Panel mt={4}>example</Panel>
  </Box>
);
